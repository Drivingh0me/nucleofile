[package]
name = "nucleofile"
version = "0.1.1"
edition = "2024"

[dependencies]
clap = {version = "4.5", features = ["derive"]}
anyhow = "1.0"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
